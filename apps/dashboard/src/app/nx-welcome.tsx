

/**
 * grouping row
 * documentation
 * storybook
 * Large dynamic grid
 * Toolbar actions/custom renderer
  NEsted Export https://stackoverflow.com/questions/39386969/nested-tables-in-a-pdf-using-jspdf-and-jspdf-autotable
  *Locallization
  *Setting filter values, column visibility, widths ,etc from code
  * oss samples
  *Row Span Col Span
  *Event listeners 
  *dd bug
  *grid span rows
  *save global filter + custom data in settings + regex in filter
  *filter tree behavior
  *hscroll on toolbar
  *dnd in docs
  *http://localhost:3000/docs/hierarchy/nested sort options
  *http://localhost:3000/docs/hierarchy/nested heights
  *http://localhost:3000/docs/hierarchy/nested filters [not being called]
  * licesning
  * useHeaderRowAsFloatingRow 
  * mui filter padding phone number




  * car model grouping
  *drop above and below
  group headers + non selectable +
  *save as you go
stylizer 
  *editors
  *datepicker
  * drag handle for filler row bug
  *dragn and drop
  *Material autocomplete,
  *Material filters
 *trader view
  *compact view
  *column icon
  *datatestId
  * column widthmodes
  *drop pin
 * material
  *custom footer
  drag drop position wrong
  *autosizing grid
 * selection modes
 * item renderers
 * item editors/Changes/Editing
 * Selection cascase dselection 
 * cell/row formatting
 * enableSelectionCheckbox
  Floating headers
  custom styling 
pin inner columns
sub-header/median row
global filter paging not updating

 * 20) Disco light updates
 * 25) Page level scrollbar
 * 13) Responsive
 * 17) Rowid column
 * 19) Infinite scrolling
 * 22) Drag range selection
 * 23) ARIA/Colorblind
 * 6) Row/Col Span
 * 
 * done:
  Column group collapser
 *   inner level renderer + height

  Nested filterbubble
 * Drag and Drop Grouping
 * empty data + footer filter not updating
 * 11) Print and Export to PDF/Excel
 * 1) Column Groups
 * 14) Column width mode
 * 2) Column Menu
 * Regex
 * 3) Horizontal Scroll Policy + Footer scrolllbar
 * 4) Footer value updates
 * 16) Multi column sort and paging
 * GlobalFilter
  * 9) QuickSearch 
 * 18) Variable row heights for all
 *  * 12) Context Menu
 * 24) Column Resizing
 * 7) Drag and Drop Reorder + 
 * 10) Preferences
 *  & Keyboard Navigation + 
 * 8) Editing fast edit
 *  * 15) itemLoadMode and filterPageSortMode
 * 5) Nested Grids

 */

import React from "react";



export function NxWelcome({ title }: { title: string }) {

  return (
    <div style={{ display: "flex", height: "100vh" }}>

    </div>
  );

}

export default NxWelcome;
